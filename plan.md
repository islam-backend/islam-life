# 🚀 خطة بناء نظام إدارة أعمالي وعملائي (Solo-OS)

هذه هي الخطة التنفيذية لبناء لوحة تحكم مخصصة لإدارة العملاء والتاسكات في صفحة واحدة (SPA)، بتصميم مستوحى من ألوان ومود ثيم **PhpStorm Dark Mode**، ومبنية من الصفر باستخدام (HTML, CSS, JS, Firebase).

---

## 🎨 الهوية البصرية وألوان الثيم (PhpStorm New Dark UI)

عشان تطلع بشكل مريح جداً للعين ومش تشتت، دي درجات الألوان اللي هتتثبت في الـ CSS واستخدام كل لون:

* **خلفية التطبيق الأساسية (`#1E1F22`):** اللون الداكن العميق للخلفية الكاملة (المساحة الفارغة).
* **خلفية القائمة والكروت (`#2B2D31`):** لون الـ Sidebar وأعمدة الكانبان وصناديق المهام، عشان تبرز عن الخلفية.
* **الخطوط الأساسية (`#DFE1E5`):** لون أبيض مائل للرمادي هادي جداً ومريح للعين أثناء القراءة الطويلة.
* **الخطوط الفرعية (`#9399A5`):** رمادي باهت لتفاصيل التاريخ، اسم العميل، والبيانات الجانبية.
* **اللون التفاعلي والأزرار (`#3574F0`):** الأزرق الصريح المريح الخاص بـ PhpStorm للأزرار النشطة والتحديد.
* **لون الحدود والفواصل (`#3F424A`):** خطوط رفيعة جداً للفصل بين القوائم والأعمدة.

---

## 🏗️ هندسة الصفحة الواحدة (Single Page Architecture)

السيستم هيتبني في صفحة واحدة مفيهاش أي ريفريش، وهتتقسم كالتالي:
1.  **Sidebar (ثابت على اليمين/الشمال):** فيه اللوجو، وزرار "الشغل والعملاء" (نشط)، وزرار للمصاريف والأكل والتمرين (Disabled حالياً).
2.  **المحتوى الرئيسي (المساحة الكبيرة الباقية):**
    * **الهيدر:** ترحيب وزرار سريع `+ إضافة مهمة لعميل` (يفتح Modal أو Form خفيفة).
    * **لوحة الـ Kanban:** مكونة من 3 أعمدة رئيسية بجانب بعضها: (📋 المطلوب / ⏳ جاري التنفيذ / ✅ تم الإنتهاء).

---

## 🛠️ مراحل التنفيذ خطوة بخطوة (The Steps)

### 🗓️ المرحلة الأولى: الهيكل العظمي والـ UI (يوم 1 - النصف الأول)
* [ ] إنشاء ملفات المشروع الأساسية: `index.html` و `style.css` و `app.js`.
* [ ] تعريف متغيرات الألوان (CSS Variables) في ملف الاستايل لتوحيد ثيم PhpStorm.
* [ ] بناء هيكل الـ Sidebar وتنسيقه ليكون ثابتاً في الشاشة.
* [ ] توزيع أعمدة الكانبان الثلاثة باستخدام (CSS Grid) أو (Flexbox) مع جعل حاويات الكروت تدعم الـ Scrolling الرأسي إذا زادت المهام.

### 🔗 المرحلة الثانية: ربط الباك-إند (يوم 1 - النصف الثاني)
* [ ] إنشاء مشروع جديد على Firebase Console وتفعيل الـ Firestore Database (Realtime Mode).
* [ ] ربط الـ SDK الخاص بـ Firebase في ملف الـ JavaScript كموديول مستقل لضمان نظافة الكود.
* [ ] تصميم هيكل المستند (Document Schema) الخاص بالتاسكات ليتضمن: (`id`, `title`, `clientName`, `status`, `createdAt`).

### 🔄 المرحلة الثالثة: جعل الداشبورد حية (Realtime Rendering) (يوم 2 - النصف الأول)
* [ ] كتابة دالة الإرسال (Form Submission) لترمي التاسك الجديد في Firebase بحالة افتراضية `status: "todo"`.
* [ ] إعداد مستمع لحظي (`onSnapshot`) يراقب قاعدة البيانات في Firebase.
* [ ] كتابة دالة بالـ JS تمسح الأعمدة وبتعيد رسم الكروت فوراً بناءً على حالتها القادمة من الداتابيز من غير ما تعمل ريفريش للمتصفح.
* [ ] تنسيق كروت المهام (العنوان، اسم العميل، زر الحذف) لتبدو كقطع مدمجة في ثيم الـ IDE.

### 🫳 المرحلة الرابعة: سحب وإفلات المهام (Drag & Drop) (يوم 2 - النصف الثاني)
* [ ] إضافة خاصية الـ `draggable="true"` للكروت البرمجية أثناء إنشائها بالـ JS.
* [ ] تفعيل مستمعات الأحداث (`dragstart`) لحفظ الـ ID الخاص بالكارت المسحوب في ذاكرة المتصفح المؤقتة.
* [ ] تفعيل مستمعات الأحداث (`dragover` و `drop`) على الأعمدة الثلاثة.
* [ ] برمجياً عند الـ `drop`: يتم أخذ الـ ID وتحديث حقل الـ `status` الخاص به في Firebase للـ Status الجديدة الخاصة بالعمود المتلقي.
* [ ] الاستمتاع بالحركة التلقائية للكارت بمجرد إفلاته لأن الـ Realtime Listener هيتولى إعادة الرسم فوراً وبسلاسة!

---

## 🔮 خطة التوسع المستقبلي (الـ Scale)
عند الرغبة في إضافة (المصاريف، الأكل، التمرين):
1.  مش هنزود صفحات HTML؛ فقط هنضيف تابات في الـ Sidebar.
2.  كل تابه هتمثل سكربت منفصل (مثال: `expenses.js`).
3.  الـ JS هيتحكم في إظهار السكشن الخاص بالتابة النشطة وإخفاء الباقي (`display: none` / `display: block`).
4.  إنشاء كوليكشنز جديدة في نفس قاعدة بيانات Firebase (`expenses`, `meals`, `workouts`).