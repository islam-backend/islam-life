# 🚀 خطة التحديث v3.2 — لوحة المؤشرات المدمجة فائقة الكثافة (Ultra-Compact Collapse Dashboard)

هذه هي الخطة التنفيذية المعدلة بناءً على مراجعة التصميم الأخير، بهدف تحويل قسم "المشاريع الجارية" إلى عنصر مدمج جداً، ذكي، وقابل للطي (Collapsible Section)، مع تصفية المشاريع لعرض الجارية فقط وحجب أي مساحات مهدرة.

---

## 🎨 البنية البصرية الجديدة (High-Density & Collapsible Matrix)

بدلاً من القوائم الطويلة الثابتة التي تستهلك مساحة الواجهة، سيتم إعادة تصميم سكشن المشاريع ليتوافق مع الفلسفة التالية:

### 1️⃣ قسم المشاريع الذكي الذاتي الطي (Collapsible Header Row)
* **رأس القسم (Section Header):** شريط بارتفاع صغير جداً يحتوي على عنوان "المشاريع الجارية" وبجانبه سهم ديناميكي (Arrow Icon) يدور بزاوية 90 درجة عند الضغط عليه (`transition: transform 0.2s ease`).
* **آلية الطي (The Collapse Mechanism):** عند الضغط على رأس القسم، يتم إخفاء أو إظهار قائمة المشاريع بالكامل فوراً عبر إضافة/إزالة كلاس `.collapsed` في الـ JavaScript (باستخدام `max-height` أو `display: none` تفادياً للهدر البصري).

### 2️⃣ معايير تصفية وعرض المشاريع (Active-Only Filter)
* **حجب التكرار والمشاريع غير النشطة:** لن يتم عرض أي مشروع منتهٍ، أو متوقف، أو مكتمل.
* **الصفوف الشريطية فائقة الصغر (Ultra-Compact Strip Rows):**
    * تقليص الارتفاع (Capped Height) ليصبح مدمجاً جداً شبيه بالمهام الجانبية الموضحة بالصورة.
    * إلغاء العناصر الكبيرة، ودمج اسم المشروع واسم العميل في سطر واحد بخط صغير وأنيق.
    * تحويل شريط التقدم الخطي (Linear Micro Bar) ليكون بمحاذاة النص أو أسفله مباشرة بارتفاع لا يتعدى `4px` للحفاظ على المظهر المضيء المنظم.

---

## 🏗️ آلية التصفح الذكي والتنقل الفوري (Bypass Routing)

* **السرعة والربط المباشر:** يظل منطق الانتقال الفوري فعالاً؛ بمجرد الضغط على الصف المدمج للمشروع (حتى وهو داخل القائمة القابلة للطي)، يتم توجيه المستخدم فوراً وبأقل من 150ms إلى لوحة الكانبان الخاصة به عبر الـ IDs الممررة (`data-client-id` و `data-project-id`).
* **حفظ حالة القائمة (State Persistence):** سيتم حفظ حالة القسم (هل هو مفتوح أم مطوي؟) داخل الـ `localStorage` بحيث إذا قمت بتحديث الصفحة أو تنقلت بين التبويبات، يفتح النظام على نفس الحالة التي تركتها (إما مغلقاً تماماً لتوفير مساحة التركيز، أو مفتوحاً).

---

## 🛠️ خطوات التنفيذ البرمجي المعدلة (V3.2 Tasks List)

### 🗓️ المرحلة الأولى: الهيكل البرمجي المرن (HTML & Collapse UI)
- [ ] تعديل سكشن `#dashboard-section` لإدراج شريط التحكم القابل للضغط الخاص بالطي والفتح ومؤشر السهم الدوار.
- [ ] تقليص أحجام كروت المشاريع الجارية وإلغاء المساحات المحيطة (Paddings/Margins) لتصبح صفوفاً شريطية متناهية الصغر وعالية الكثافة البصرية.
- [ ] إضافة شريط التقدم الدقيق (Micro-Rail `4px`) باللون الأخضر المضيء `--success`.

### 📊 المرحلة الثانية: فلترة البيانات والـ Realtime Logic
- [ ] تحديث كود الـ Loop المسؤول عن جلب المشاريع في الـ JS، ليعمل على تصفية واستبعاد أي مشروع حالته غير "جاري" أو "نشط".
- [ ] ربط الـ Toggle Event في ملف `app.js` ليقوم بتبديل كلاس الطي وحفظ القيمة (True/False) في الـ `localStorage`.

### 🔗 المرحلة الثالثة: ضبط زر الرجوع والإنتاجية
- [ ] التأكد من أن منطق الـ `History Stack` لزر الرجوع (`toolbar-back-btn`) يعمل بسلاسة تامة ويعيدك للداشبورد بنفس حالة الطي التي كانت عليها قبل الانتقال للكانبان.