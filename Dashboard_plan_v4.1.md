# 🚀 خطة التحديث v3.7 — تابة "وضع التركيز" ومؤقت البومودورو المركزي (Focus Mode Tab)

هذه هي الخطة التنفيذية المعتمدة نهائياً لبناء الداشبورد وتطبيق البومودورو (v3.7). تم الإبقاء على "صباح الخير" وضغط كروت الإحصائيات وتوحيد عمود الداشبورد، مع نقل البومودورو إلى تابة منفصلة تماماً تصبح بمثابة "غرفة التركيز العزلية".

---

## 🎨 البنية المعمارية وتوزيع التبويبات (SPA Navigation Flow)

### 1️⃣ إضافة تابة جديدة في الـ Sidebar:
*   يتم إضافة تيار تصفح جديد باسم **"⏱️ جلسة التركيز"** أو **"Focus Mode"** داخل القائمة الجانبية.
*   عند الضغط عليها، يتم إخفاء كل السكاشن وإظهار سكشن مركزي مخصص `#focus-section`.

### 2️⃣ واجهة البوكس المركزي (The Central Focus Workspace)
التابة الجديدة ستكون عبارة عن مساحة هادئة جداً، تتمحور حول **بوكس مركزي متناسق وأنيق** في منتصف الشاشة (`display: flex; justify-content: center; align-items: center; min-height: 70vh;`):
*   **خلفية وهيكل البوكس:** يتبع ثيم PhpStorm (`--bg-card: #2B2D31`) مع حدود ناعمة ومضيئة بحجم متناسق لا يشتت العين.
*   **قائمة اختيار المشروع الذكية (Project Selector Dropdown):** 
    *   سؤال تفاعلي في قمة البوكس: **"فيبدأ العمل على مشروع ماذا اليوم؟ 🎯"**
    *   قائمة منسدلة (Dropdown) مخصصة ونظيفة تسحب ديناميكياً المشاريع الجارية فقط من Firestore لتختار منها المشروع والتاسك المستهدفة قبل البدء.
*   **مفاتيح تبديل الوقت (Duration Toggles):**
    *   زرين لتحديد نوع الجلسة: **[جلسة سريعة: 25 دقيقة / 5 دقائق راحة]** أو **[تركيز عميق: 60 دقيقة / 10 دقائق راحة]**.
*   **العداد الرقمي المركزي (The Big Digital Counter):**
    *   أرقام ضخمة، واضحة ومضيئة في السنتر تعكس الوقت المتبقي (`25:00` أو `60:00`) وتتغير ألوانها ديناميكياً (أحمر `--danger` للشغل / أخضر `--success` للراحة).
*   **شريط التقدم الخطي الدائري أو السفلي (Micro Progress Rail):**
    *   خط بارتفاع `4px` أسفل البوكس تماماً يتقلص مع مرور الثواني ليعطيك مؤشراً بصرياً لحظياً لمدى تقدم الجلسة.
*   **أزرار التحكم الأساسية:** (ابدأ الجلسة ▶️ / إيقاف مؤقت ⏸️ / إعادة تعيين 🔄).

---

## 🗄️ منطق البيانات والتتبع (Firestore Realtime Aggregations)

*   بمجرد اختيار المشروع وضغط "ابدأ الجلسة"، يقفل الـ Dropdown ويتحول البوكس لحالة التركيز القفلية (Locked Mode).
*   عند اكتمال الجلسة بنجاح وسماع صوت التنبيه، يقوم موديول الـ JS بإرسال تحديث تلقائي مباشر لـ Firestore ليقوم بزيادة العداد:
    *   داخل مستند المشروع المحدد: `totalMinutesSpent += 25` أو `60`.
    *   داخل مستند التاسك المحددة: `taskMinutesSpent += 25` أو `60`.

---

## 🛠️ خطوات التنفيذ البرمجي لـ v3.7 (Action Items)

### 🗓️ المرحلة الأولى: هيكلة تابة التركيز والبوكس المركزي (HTML & CSS)
- [ ] إضافة التبويب الجديد في الـ Sidebar وإعداد الـ DOM Container الخاص بـ `#focus-section`.
- [ ] بناء وتنسيق البوكس المركزي وتوسيطه في الشاشة بالكامل باستخدام Flexbox.
- [ ] تصميم القائمة المنسدلة (Dropdown) الخاصة باختيار المشاريع والتاسكات لتكون متناسقة تماماً مع ألوان `--bg-main` و `--border-color`.
- [ ] تعديل كروت إحصائيات الداشبورد الرئيسية (التابة الأولى) لتصبح شريطية مدمجة كلياً كما اتفقنا في `v3.5`.

### 📊 المرحلة الثانية: محرك العداد التتبعي (The JS Timer Engine)
- [ ] كتابة كود الـ Timer في ملف `focus.js` ودعم منطق الـ Switch بين الجلستين (25 دقيقة و 60 دقيقة).
- [ ] ربط قائمة الاختيار (Dropdown) بـ Firestore لجلب أسماء المشاريع الجارية لحظياً عند فتح التابة.
- [ ] برمجة الـ Database Update لتوثيق الدقائق المستغرقة فور انتهاء العداد وتفعيل الـ Premium Toast Alert.